gtest_dep = dependency('gtest', main: true, required: false)

subdir('api/')
subdir('log/')
subdir('utils/')

subdir('query/')
subdir('state/')
subdir('resource-graph/')
subdir('pkg-manager/')

subdir('controller/')
subdir('controllers/')
subdir('hypha/')

subdir('bindings/')
